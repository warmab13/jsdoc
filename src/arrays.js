
/**
 * List user ages
 * @type {Array}
 */
const ages = [19, 29, 30, 35, 45, {}, true ];


/**
 * Array of user's scores
 * @type {Array<Number>}
 */
const scores = [10, 199.99, 299.99, 3000]

/**
 * Array of Number and strings
 * @type {Array<Number | string>}
 */
const scoresAndString = [10, "10", "30", 3000]

/**
 * Array of names
 * @type {Array<string>}
 */

const names = ["Ryan", "John", "Joe"]